const initState = {
  loader: false,
  modal: {
    message: null,
    show: false,
  },
};

const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
